# T04 Closure Memo — Operational Translation

## Objective and inputs

T04 operationalized the T03-selected model without revisiting selection, tuning, features, or EDA. Inputs were `selected_model.json`, the saved holdout comparison and segment evidence, the prediction set in original order, and the submission generated by the selected full-data pipeline. Outputs are the validated challenge submission, validation report, multiplicative error table, full-versus-early sensitivity table, and implementation notes.

## Submission validity and integrity checks

**Operational status: VALID.** The selected identifier remains **M2, full-contemporaneous HistGradientBoostingRegressor**, with seed 202503 and primary validation RMSLE 0.5286. T04 verifies that this metadata matches the sole selected T03 comparison row. The 64,502-row submission has exact columns `Name,Stars`, preserves prediction-set Name order, contains no null, non-finite, or negative prediction, has no accidental index, and retains continuous precision. Predictions were clipped at zero by the reproducible inference function and attached positionally; there was no Name join or manual editing. File hashes are stored in the validation report.

## Operational interpretation and sensitivity

Exponentiating RMSLE 0.5286 gives about **1.70×** as intuition for the RMS log-error scale, not a guarantee or a claim that every row has the same error. Median absolute log error 0.3296 corresponds to about **1.39×**; 83.5% of held-out rows are within 2× and 99.9% within 10×.

The approved early-information M1 view scores 0.9429 versus 0.5286 for full M2: early information is **0.4143 RMSLE higher** (78.4% higher relative to M2). Conversely, M2's reduction versus M1 is 43.9%. This comparison does not isolate individual feature effects, but it demonstrates that much of the predictive performance depends on contemporaneous size, activity, recency, and lifecycle proxies. The submission therefore estimates snapshot-time Stars and must not be presented as launch-time forecasting or causal guidance.

## Dominant risks and acceptance decision

Dominant error risks are the sparse 10,000+ tail and 1,000–9,999 band, activity–popularity mismatches, historically popular projects with weak current proxies, rare/unseen languages, and unusual young or zero-issue profiles. Aggregate validation error was not elevated for missing descriptions or archived repositories, but that does not eliminate row-level exceptions. Delivery also depends on unchanged input schema/content, the pinned environment, the fixed reference date, and a clean full rerun after any input change.

**PASS — Gate 04 is closed and T05 is unlocked.** Every T04 claim above traces to a saved artifact, the sensitivity is complete, and no unsupported capacity policy or causal recommendation was introduced.

Before presentation, the human consultant must decide whether to submit the validated file, how prominently to qualify the contemporaneous timing, whether the 1.70× intuition is sufficiently clear alongside its caveat, and how much emphasis to place on high-tail risk. The human retains the final recommendation and presentation wording.
